# LifeOS-Shop-統一版 專案狀態

> Claude 新對話**只讀這份**。其他進度檔一律不再產生，避免誤讀。
> 排序：舊 → 新（流水帳式，最底下永遠是最新）。
>
> **降級規則（重要）**：
> 每次對話結束前把「本次完整記錄」寫入底部。下次對話開始時要降級：
> - **新的一層簡化** = (舊一層簡化 + 舊本次完整) **合併**後再簡化
> - **新的二層簡化** = (舊二層簡化 + 被擠下來的舊一層簡化片段) **合併**後更簡化
> - 絕對保留：**檔案路徑、函式名、坑點、為何這樣做（決策脈絡）**
> - 可刪：冗詞、重複敘述、過時的過程討論
> - 核心思想：簡化時**要合併不是單獨壓縮**，才能保留脈絡、Claude 回來看不會漏掉早期重點

---

## 📍 當前狀態（一句話）
新租戶 Firebase 串成、admin 可整段貼 Google 範例；roster/weekly/office 預設資料去識別化；線上版已同步最新 commit。

## 🌐 入口
- 線上首頁：https://emineokur221105-spec.github.io/lifeos-shop/
- Admin：`?admin=0308`
- 租戶入口：`?t=<代號>`（例：`?t=demo-qinre-main`）
- Repo：https://github.com/emineokur221105-spec/lifeos-shop

---

## 🗺️ 檔案職責地圖（kabe 要改東西看這邊）

### 頁面
| 我想改什麼 | 檔案 |
|-----------|------|
| 首頁文案/按鈕/路由 | `src/index.html` |
| Admin 後台（租戶 CRUD / 白名單） | `src/admin.html` |
| 人員管理（上班/離職/地點模板/排版輸出） | `src/roster.html` |
| 分紅（股東 %／週收入／支出） | `src/office.html` |
| 週結業績（個人明細／查帳表／合併名稱／帳戶） | `src/weekly.html` |
| 門店排班 HTML 外殼 | `src/shop.html` |
| 系統設定（價格表／阿姨名單／匯入匯出） | `src/settings.html` |

### shop.html 內部模組（在 `src/shop/shop_data/`）
| 要改什麼 | 檔案 |
|---------|------|
| 入口 / bootTenant 串接 | `main.js` |
| 全域狀態（staffData/REGIONS/services 等） | `state.js` |
| Firebase v11 helper 層 | `shop-db.js` |
| 系統設定（雲端覆蓋） | `config.js` |
| 小工具（日期/營業日/鬧鐘/顏色/價格表讀取） | `utils.js`（營業日切換點 `BUSINESS_DAY_CUTOFF_HOUR` 在這） |
| 排班時間軸 / 拖拉 / 鬧鐘 | `schedule.js` |
| 結算邏輯 / 阿姨帳字串 / 經紀費 | `settlement.js` |
| shop 的週結 tab | `weekly.js` |
| UI 主控（tab 切換 / CRUD / 截圖 / 縮放 / 緊急清空 / exposeGlobals） | `app.js` |

### 共用模組（在 `src/core/`）
| 要改什麼 | 檔案 |
|---------|------|
| 防盜（白名單 / F12 / DevTools 門檻） | `security.js`（門檻 `THRESHOLD` 在這；`?debug=1` 旁路） |
| 租戶開機流程 | `tenant-boot.js` |
| 主 Firebase 連線設定 | `main-firebase-config.js`（實際 config 另存 `LOCAL_SECRETS.md`） |
| 租戶清單 / 白名單讀取 | `tenant-loader.js` |
| 小工具（toast/escape/copy/租戶 namespace） | `common.js` |

### 建置 / 部署
| 要改什麼 | 檔案 |
|---------|------|
| 混淆流程 / HTML 內嵌 script 處理 | `build.js` |
| 本機一鍵部署 | `一鍵部署.bat` |
| GitHub Actions 自動部署 | `.github/workflows/deploy.yml` |

### 文件
| 檔案 | 用途 |
|-----|------|
| `README.md` | Repo 基本說明 |
| `SETUP_GUIDE.md` | kabe 的操作手冊 / 新增租客 SOP |
| `LOCAL_SECRETS.md` | 主 Firebase config（gitignore，不上雲） |
| `claude週結/ARCHITECTURE.md` | 舊版週結系統架構筆記（參考用） |
| **`專案狀態.md`（本檔）** | **唯一狀態來源，Claude 新對話必讀** |

---

## 📦 二層簡化（更早）— weekly / shop / security 三大強化

- **weekly.html**：F1 查帳摺疊 / F2 個人搜尋 / F3 合併名稱（`weekly_data/merges`，`resolveMaster()` cycle 防呆、`applyMerges()` 展開 rawPersonData，🔗 選 master / ✂️ 拆開、明細別名紫 tag）
- **shop.html**（`src/shop/shop_data/`）：截圖只留 summary + 人員卡、暫解 view-settle overflow 用 html2canvas `scrollHeight`（`app.js` `downloadScreenshot`）；阿姨帳換行改空白（`settlement.js`）；營業日凌晨 5 點前算前一天（`utils.js` `BUSINESS_DAY_CUTOFF_HOUR = 5`）
- **security.js**：DevTools `THRESHOLD` 160 → 300px（減少縮放誤觸）
- **進度檔**：刪 4 份舊進度檔，統一為 `專案狀態.md` 三層降級制
- **commits**：`3aa8bb5` weekly / `d2e8446` `960e81a` shop / `5eda780` security / `f02090d` 營業日 / `779ccbd` 進度檔

## 📋 一層簡化（上次）— 系統設定密碼保護（2026-04-21）

- **密碼流程**：`src/index.html` 第 5 顆 `.btn-settings` 按鈕 → `renderSettingsPwdForm()` inline 密碼框（不用 prompt）→ `sha256Hex` 比對 → `crypto.randomUUID()` 產一次性 token 存 `sessionStorage['settings_unlock_token_<code>']` → 跳 `settings.html?t=` → settings 進頁 `removeItem` 消費 token
- **預設密碼**：`DEFAULT_SETTINGS_PWD_HASH = 'f3e055913a0b1eb0f07317896f9a1bc466b9a50db85a7f882f3ffde9ffb23aca'`（1357 的 SHA-256），全租戶共用
- **租戶覆寫**：Admin 後台 `tenants/<code>/settingsPasswordHash`，🔓 按鈕還原預設
- **Admin 密碼**：`ADMIN_KEY_HASH` 對齊 `0308` 的 SHA-256（`d86d84d9...`）；`package.json` dev 改 `-o /admin.html?admin=0308`
- **坑**：密碼框位置迭代 3 次（settings prompt → 唯讀 banner → 最終 index inline）。教訓：先問「誰看什麼」再寫 code
- **一次性 token 理由**：sessionStorage 旗標同 tab 會常駐 → 改 `crypto.randomUUID()` 一次消費防直接貼 URL 繞（改前端仍可繞，P2 Firebase Auth 才解）
- **commits**：`7c8dae5` feat / `e3f2ab9` docs

---

## 📝 本次完整記錄（2026-04-21 續 2）— admin 彈性解析 + 預設資料去識別化 + 新租戶部署

### 背景
- 前一輪做完密碼保護後沒 push，`origin/main` 落後 local 2 commits → kabe 在線上版看不到新功能
- kabe 新開了 Firebase 專案測試多租戶，想直接在 admin 整段貼 Google console 複製的範例（含 `import` / `const firebaseConfig = {...}` / `getAnalytics`）
- 同時要開始招新租戶，所以 roster / weekly / office 裡面 moneyhouse 的實際業務資訊不能沿用

### 三個程式改動（commit `7b4bc57`）

**1. Admin Firebase config 彈性解析（`src/admin.html`）**
- 新增 `parseFirebaseConfigFlexible(raw)` 函式（`handleTenantSave` 前）：
  1. 先試 `JSON.parse(raw)`
  2. 優先 match `/firebaseConfig\s*=\s*/`，從 `m.index + m[0].length` 開始找第一個 `{`（**避開最上面 `import { initializeApp } from ...` 的大括號干擾**，這是第一版 bug）
  3. 沒找到 `firebaseConfig = ` 就 fallback 抓第一個 `{`
  4. 配對大括號（depth 計數）→ key 加雙引號（`/([{,]\s*)([a-zA-Z_$][\w$]*)\s*:/`）→ 單引號轉雙引號 → 去尾逗號 → `JSON.parse`
- `handleTenantSave` 把 `JSON.parse(cfgRaw)` 換成 `parseFirebaseConfigFlexible(cfgRaw)`
- field-hint 文案：「可直接整段貼 Firebase console 複製的範例（含 import / const firebaseConfig = {...} / getAnalytics 都沒關係），系統會自動抽出 {...} 部分」
- **前置**：新 Firebase 要先在 console 啟用 Realtime Database，複製的 config 才會帶 `databaseURL`（後端驗證會擋）

**2. tenant-loader 修 bug（`src/core/tenant-loader.js`）**
- `loadTenant()` return 物件原本漏了 `firebaseConfig` 欄位
- settings.html 要讀 `tenant.firebaseConfig` 初始化第二個 Firebase app（設定畫面自己的 db），漏這行會報「租戶 Firebase 設定未載入」
- 修復：return 加 `firebaseConfig: tenant.firebaseConfig,`

**3. security.js 加 debug 旁路（`src/core/security.js`）**
- `init()` 加 `?debug=1` URL 參數 + sessionStorage `lifeos_debug='1'` 旁路
- 旁路生效時 `console.warn` 提示後直接 return（跳過 `installKeyBlocker` + `installDevToolsDetector`）
- **⚠️ 上線前要移除**（kabe 明確說這是測試期暫時用）

### 預設資料去識別化（commit `6f67f93`）

**關鍵**：三處都是 fallback（Firebase `snap.val()` 為空才套用），**不會覆蓋 moneyhouse 已存資料**。

- **`src/roster.html`** — 
  - `DEFAULT_LOCATIONS`：`民權路 / 莊二街 / 北新街` → `某某街 / 某某路 / 某某巷`（badge 改 A/B/C）
  - `DEFAULT_TEMPLATES.detailed.header`：「🔥紙房子🔥定點 / 紙房子1線-派工下單【東京】 / ⭐️桃園區」→ 「🔥定點上班表🔥 / 下單請敲 ➡️➡️@你的派工名稱 / ⭐️地區」
  - `DEFAULT_TEMPLATES.detailed.sectionHead`：移除「❌❌❌不接生客 / 需成功消費3次以上」→ 只留「今日 ({count}) 位上班」
  - `DEFAULT_TEMPLATES.detailed.footer`：整段實際店規（入珠洗澡規則 / 罰款金額 / 爆頭金額）→ 「※ 請顧客自行確認服務內容 / ※ 相關規範依現場公告為準 / ※ 詳情請洽派工人員」
  - `concise` 全留（原本就不敏感）

- **`src/weekly.html`** — `billWarningTemplate` 預設：`匯錯不補，請仔細確認` → `如有疑義請與負責人確認`；`預計入款：週一存入` → `入款時間：依實際情況為準`

- **`src/office.html`** — `defaultData`：
  - 股東 `OG 67% / C 28% / 黑 5%` → `股東甲 50% / 股東乙 30% / 股東丙 20%`
  - 支出 `做圖軟體月費 450 / 公司房租代墊 26233 / 團隊聚餐 1200` → `辦公用品 500 / 設備維護 2000 / 雜項支出 800`（paidBy 同步改甲乙）

### 坑 / 教訓
- **線上版落後 2 commits**：kabe 說「串好了但看不到系統設定」→ 追查發現 `origin/main` 落後 local。寫完功能後要 `git push`，GitHub Actions 會自動 `npm run build` + deploy
- **`parseFirebaseConfigFlexible` 第一版抓錯大括號**：原本直接抓「第一個 `{`」，遇到 Google 完整範例會抓到 `import { initializeApp }` 的大括號。修成先找 `firebaseConfig =` 之後才抓

### 本輪 commits
```
7b4bc57 feat(admin): Firebase config 彈性解析 + tenant-loader bug 修 + security debug 旁路
6f67f93 chore: roster / weekly / office 預設資料去識別化（新租戶 fallback）
```

---

## 🚀 未來可完善（非緊急）

| 優先 | 項目 | 說明 |
|------|------|------|
| P1 | 移除 `?debug=1` 旁路 | 上線穩定後把 `security.js` 的 debug 旁路拔掉 |
| P2 | Admin 密碼升級 | 從 SHA-256 驗 `?admin=0308` 換成 Firebase Auth 登入 |
| P2 | localStorage 租戶 namespace | 同瀏覽器切多租戶時資料不互撞（`core/common.js` 已有 `lsKey()` helper，逐頁套用即可） |
| P2 | 統一 CSS / 導航列 | 7 個頁面目前各自為政，視覺不一致 |
| P2 | 兩套週結系統整併 | `weekly.html` + `shop.html` 的週結 tab（商業決策，未定） |
| P3 | 合併名稱「別名管理」頁 | weekly 的合併規則目前只能從 card 操作，未來加一個集中管理頁 |
| P3 | 合併規則匯出 / 備份 | `weekly_data/merges` 目前只在 Firebase，沒有下載備份機制 |
