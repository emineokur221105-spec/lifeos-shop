# LifeOS-Shop-統一版 專案狀態

> Claude 新對話**只讀這份**。其他進度檔一律不再產生，避免誤讀。
> 排序：舊 → 新（流水帳式，最底下永遠是最新）。
>
> **降級規則（重要）**：
> 每次對話結束前把「本次完整記錄」寫入底部。下次對話開始時要降級：
> - **新的一層簡化** = (舊一層簡化 + 舊本次完整) **合併**後再簡化
> - **新的二層簡化** = (舊二層簡化 + 被擠下來的舊一層簡化片段) **合併**後更簡化
> - 絕對保留：**檔案路徑、函式名、坑點、為何這樣做（決策脈絡）**
> - 可刪：冗詞、重複敘述、過時的過程討論
> - 核心思想：簡化時**要合併不是單獨壓縮**，才能保留脈絡、Claude 回來看不會漏掉早期重點

---

## 📍 當前狀態（一句話）
Phase 3 全部驗收完成。roster / office / weekly / shop / settings 全上線可用；settings 已加客戶端密碼保護。

## 🌐 入口
- 線上首頁：https://emineokur221105-spec.github.io/lifeos-shop/
- Admin：`?admin=0308`
- 租戶入口：`?t=<代號>`（例：`?t=demo-qinre-main`）
- Repo：https://github.com/emineokur221105-spec/lifeos-shop

---

## 🗺️ 檔案職責地圖（kabe 要改東西看這邊）

### 頁面
| 我想改什麼 | 檔案 |
|-----------|------|
| 首頁文案/按鈕/路由 | `src/index.html` |
| Admin 後台（租戶 CRUD / 白名單） | `src/admin.html` |
| 人員管理（上班/離職/地點模板/排版輸出） | `src/roster.html` |
| 分紅（股東 %／週收入／支出） | `src/office.html` |
| 週結業績（個人明細／查帳表／合併名稱／帳戶） | `src/weekly.html` |
| 門店排班 HTML 外殼 | `src/shop.html` |
| 系統設定（價格表／阿姨名單／匯入匯出） | `src/settings.html` |

### shop.html 內部模組（在 `src/shop/shop_data/`）
| 要改什麼 | 檔案 |
|---------|------|
| 入口 / bootTenant 串接 | `main.js` |
| 全域狀態（staffData/REGIONS/services 等） | `state.js` |
| Firebase v11 helper 層 | `shop-db.js` |
| 系統設定（雲端覆蓋） | `config.js` |
| 小工具（日期/營業日/鬧鐘/顏色/價格表讀取） | `utils.js`（營業日切換點 `BUSINESS_DAY_CUTOFF_HOUR` 在這） |
| 排班時間軸 / 拖拉 / 鬧鐘 | `schedule.js` |
| 結算邏輯 / 阿姨帳字串 / 經紀費 | `settlement.js` |
| shop 的週結 tab | `weekly.js` |
| UI 主控（tab 切換 / CRUD / 截圖 / 縮放 / 緊急清空 / exposeGlobals） | `app.js` |

### 共用模組（在 `src/core/`）
| 要改什麼 | 檔案 |
|---------|------|
| 防盜（白名單 / F12 / DevTools 門檻） | `security.js`（門檻 `THRESHOLD` 在這） |
| 租戶開機流程 | `tenant-boot.js` |
| 主 Firebase 連線設定 | `main-firebase-config.js`（實際 config 另存 `LOCAL_SECRETS.md`） |
| 租戶清單 / 白名單讀取 | `tenant-loader.js` |
| 小工具（toast/escape/copy/租戶 namespace） | `common.js` |

### 建置 / 部署
| 要改什麼 | 檔案 |
|---------|------|
| 混淆流程 / HTML 內嵌 script 處理 | `build.js` |
| 本機一鍵部署 | `一鍵部署.bat` |
| GitHub Actions 自動部署 | `.github/workflows/deploy.yml` |

### 文件
| 檔案 | 用途 |
|-----|------|
| `README.md` | Repo 基本說明 |
| `SETUP_GUIDE.md` | kabe 的操作手冊 / 新增租客 SOP |
| `LOCAL_SECRETS.md` | 主 Firebase config（gitignore，不上雲） |
| `claude週結/ARCHITECTURE.md` | 舊版週結系統架構筆記（參考用） |
| **`專案狀態.md`（本檔）** | **唯一狀態來源，Claude 新對話必讀** |

---

## 📦 二層簡化（更早）
_無資料_

## 📋 一層簡化（上次）— weekly / shop / security 三大強化

**weekly.html 三大加強**
- F1 查帳總表可摺疊：`tableViewWrapper` display 切換
- F2 個人明細搜尋：`personSearchInput` oninput 即時過濾
- F3 合併名稱（方案 A）：
  - 資料：全域 `weekly_data/merges: { alias: master }`（跨週共用）
  - `resolveMaster()` cycle 防呆；`applyMerges()` 把 rawPersonData 依規則展開到 master，每筆保 `from=alias`
  - 合併卡 `.is-merged` 紫色漸層 + 🔗 徽章；🔗 彈窗選 master、✂️ 拆開；明細別名紫 tag
  - 合併時自動接最終 master + 重指向 alias 的規則（避免鏈結破碎）

**shop.html**（`src/shop/shop_data/`）
- 截圖修正（`app.js` `downloadScreenshot`）：只保留 summary + 人員卡；暫解 `view-settle` / `.settle-container` 的 overflow/height，用 `windowHeight/height: scrollHeight` 給 html2canvas 抓完整捲動範圍
- 阿姨帳字串（`settlement.js`）：`4/10\n張三150` → `4/10 張三150`（單行空白分隔）
- 營業日邏輯（`utils.js` `getTodayDateStr()`）：`BUSINESS_DAY_CUTOFF_HOUR = 5`，凌晨 5 點前算前一天（上班 13:00~03:00 跨日用）

**security.js**：DevTools `THRESHOLD` 160 → 300px（減少瀏覽器縮放 / scale 誤觸）

**進度檔清理**：刪 REFACTOR_PROGRESS.md / FUTURE_IMPROVEMENTS.md / 現況與未來.txt / 新增 文字文件.txt；`專案狀態.md` 成唯一來源

**commits**：`3aa8bb5` weekly 三大加強 / `d2e8446` shop 截圖 + 阿姨帳 / `960e81a` 截圖 overflow / `5eda780` DevTools 門檻 / `f02090d` 營業日 / `779ccbd` 進度檔統一

## 📝 本次完整記錄（2026-04-21 續）— 客戶端系統設定密碼保護

### 核心需求
- 客戶在 `/?t=<code>` 租戶首頁能看到「⚙️ 系統設定」按鈕（第 5 顆），但點進去要密碼
- 密碼 UI **不用瀏覽器原生 prompt**，要跟 index 輸入租戶代號那個輸入框同風格（inline）
- 提示**只寫「忘記密碼請聯繫管理員」**，預設密碼 1357 不能寫在頁面（避免爆雷）
- 預設密碼 `1357` 全租戶通用；Admin 可在後台改成該租戶專屬密碼覆寫

### 最終流程（4 個檔案）

**`src/index.html`**
- 新增第 5 顆按鈕 `⚙️ 系統設定`（CSS `.btn-settings` = `#34495e → #7f8c8d` 深灰漸層）
- 新增 `sha256Hex`、`DEFAULT_SETTINGS_PWD_HASH = 'f3e055913a0b1eb0f07317896f9a1bc466b9a50db85a7f882f3ffde9ffb23aca'`（`1357` 的 SHA-256）
- 原本 inline 渲染的 5 顆按鈕抽成 `renderHome(tenant, settingsPwdHash)`；另加 `renderSettingsPwdForm(tenant, hash)` —— 點 `#openSettings` 後把 app 的 innerHTML 換成 inline 密碼框（沿用 `.entry-form` / `.msg info` 樣式）
- bootTenant 之後 async 讀 `tenants/<code>/settingsPasswordHash`（沒自訂就用預設 hash）
- 密碼對 → 產生 nonce（`crypto.randomUUID()`）存 `sessionStorage['settings_unlock_token_' + code]` → `location.href = './settings.html?t=...'`
- 密碼錯 → 紅字「密碼錯誤」inline 顯示、input 清空

**`src/settings.html`**
- 進頁第一件事：讀 `sessionStorage['settings_unlock_token_' + code]` → 有就**立刻 removeItem 消費** → 沒就 `location.replace` 回 `./?t=...`
- 其他內容完全沒變（settings 畫面不再內嵌密碼鎖，也沒有唯讀模式）

**`src/admin.html`**
- `renderTenantEditor`：新增「Settings 密碼」欄位（type=password, autocomplete=new-password），條件文案：
  - `hasPwd=true` → 綠字「目前已設定自訂密碼（已覆寫預設 1357）」+ 🔓 還原預設 1357 按鈕（`data-act="tenant-clear-pwd"`）
  - `hasPwd=false` → 橘字「目前使用預設密碼 1357」
- `handleTenantSave`：保留現有 `settingsPasswordHash`；只有 `pwdInput` 非空才 SHA-256 覆寫
- `handleClearPassword`：`fbDb.remove('tenants/' + code + '/settingsPasswordHash')`
- `ADMIN_KEY_HASH` 改成 `d86d84d945596a7836b26b9e06e05042ba9617bac697b793b008fb4c4f7c218a`（`0308` 的 SHA-256；進度檔本來就寫 `?admin=0308`，這次把程式碼的 hash 對齊到這個密碼）

**`package.json`**
- `dev` script：`-o /?admin=0308` → `-o /admin.html?admin=0308`（原本會落到 `index.html` 的 `if (tenantCode)` 外的分支顯示租戶輸入框，因 index 根本不處理 `?admin` 參數）

### 踩過的坑（重要）
- **第一版密碼關卡放錯地方**：最初把密碼檢查寫在 `settings.html` 開機流程（進頁就 prompt）→ kabe 回「客戶應該看得到設定頁，不是進不去」→ 改成整頁唯讀 + 橘色 banner 解鎖 → kabe 再回「要密碼框在 index、不是在 settings」→ 才定案「index 密碼框 + settings 只檢 token」。**教訓：需求對焦要先問清「誰看得到、要看到什麼」，別急著寫 code**。
- **sessionStorage 旗標會被同 tab 重複利用**：原本 `settings_unlocked_<code>='1'` 在同一個 tab 設過就常駐 → 直接貼 `settings.html?t=...` 的 URL 可繞過。改用**一次性 token**（`crypto.randomUUID()` 產生、settings 進頁 `removeItem` 消費），結果：重整 settings 也會被踢回 index（因為 token 已消）。**安全層級足夠 UX 擋誤觸，駭客仍可改前端繞，但這是 P2 Firebase Auth 才解的範圍**。
- **index 路由沒處理 `?admin=0308`**：`index.html` 只 `if (tenantCode)` 分流，`?admin` 從頭到尾都是 `admin.html` 自己的事。dev script 錯寫 `-o /?admin=0308` → 會跑到 index 的「顯示輸入框」分支。
- **admin 密碼 hash 不一致**：進度檔寫 `?admin=0308` 但程式碼 `ADMIN_KEY_HASH` 是另一個值。kabe 要求對齊到 0308。

### 本輪 commits
```
7c8dae5 feat: 系統設定密碼保護 + admin 設定入口
```

---

## 🚀 未來可完善（非緊急）

| 優先 | 項目 | 說明 |
|------|------|------|
| P2 | Admin 密碼升級 | 從 SHA-256 驗 `?admin=0308` 換成 Firebase Auth 登入 |
| P2 | localStorage 租戶 namespace | 同瀏覽器切多租戶時資料不互撞（`core/common.js` 已有 `lsKey()` helper，逐頁套用即可） |
| P2 | 統一 CSS / 導航列 | 7 個頁面目前各自為政，視覺不一致 |
| P2 | 兩套週結系統整併 | `weekly.html` + `shop.html` 的週結 tab（商業決策，未定） |
| P3 | 合併名稱「別名管理」頁 | weekly 的合併規則目前只能從 card 操作，未來加一個集中管理頁 |
| P3 | 合併規則匯出 / 備份 | `weekly_data/merges` 目前只在 Firebase，沒有下載備份機制 |
